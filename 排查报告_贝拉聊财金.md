# 排查报告：贝拉聊财金「抓取数据与频道真实不相符」

- **排查对象**：`kol_data.json` #21 贝拉聊财金 及其在 `output/data.json` / `output/report.html` 中的展示内容
- **排查时间**：2026-08-16
- **核查方法**：审查抓取链路源码（`src/fetcher.py` → `src/deepseek_analyzer.py` → `src/report_generator.py`）+ 离线审计（`src/authenticity_check.py`）+ web_search 在线核实频道归属与订阅数

---

## 一、结论（TL;DR）

**不是抓取失败，而是抓取成功后数据被代码故意覆盖。**

RSS 抓取链路本身正常：`channel_id` 正确指向贝拉聊财金官方频道，真实视频标题、链接、发布日期都抓到了（存在 `original_title` 字段）。但 `fetcher.py` 的 `enrich_with_mock_content()` 用内置的中文虚构标题/摘要**覆盖**了真实内容，下游 DeepSeek 多空研判与战报展示全部基于虚构文本，导致"与频道真实完全不符"。

## 二、证据链

### 2.1 频道定位正确（channel_id 无张冠李戴）

| 项 | kol_data.json 旧值 | 在线核实结果 |
|---|---|---|
| channel_id | `UCVomjkM_t0EcctTWSE1Jvxg` | ✅ 正确：YouTube 官方结构化数据 `sameAs` 确认该 ID 即「贝拉聊财金」 |
| channel_url | `https://www.youtube.com/@user-bella-finance` | ❌ 错误：真实 handle 为 **@bellafinance**（已修正） |
| handle | `@贝拉聊财金` | ❌ 与 URL 不一致（已修正为 @bellafinance） |
| fans | `40W+` | ❌ 失实：实际约 **20.3 万**订阅（已修正为 20W+） |

### 2.2 RSS 抓取成功，但真数据被 Mock 覆盖

`output/data.json` 中贝拉聊财金的 3 条记录：

| # | 报告的 title（展示用，**虚构**） | original_title（**真实抓取**） | 链接/日期 |
|---|---|---|---|
| 1 | AI超级碗与万亿宣言！机构史诗级抛售潮来了？ | **25年来最贵的一笔债！** | ✅ 真实（watch?v=atry_X1AdbQ，08-15） |
| 2 | 中美顶级金融暗战：这个周末发生了什么 | **接下来： 三重利好共振！！！** | ✅ 真实（08-14） |
| 3 | 美股七巨头财报周：谁在裸泳一目了然 | **答卷：属于典型的"踩线及格"！川普酝酿"大招」…** | ✅ 真实（08-13） |

- 展示 `title` 全部命中 `fetcher.py` 中写死的 `MOCK_TITLES_POOL["贝拉聊财金"]` 三条语料；
- `summary` 为凭空编造的中文文案；真实视频标题只在 `original_title` 里"留底"，**没有任何下游模块使用它**。

### 2.3 污染传播路径

```
RSS 真实条目 ──► enrich_with_mock_content() 用 mock 覆盖 title/summary
                        │
                        ▼
        DeepSeek / 启发式多空研判（输入 = 假标题+假摘要）
                        │
                        ▼
        report.html / digest（展示 item.title）→ 微信推送
```

→ 战报中贝拉聊财金的"空头/多头/中性"判定、`reason`、`advice` **全部与真实频道观点无关**。

## 三、修复内容（2026-08-16）

1. **`src/fetcher.py` `enrich_with_mock_content()`**：有真实 RSS 条目时，`title`/`summary`/`link`/`published` 全部使用频道真实数据，不再用 MOCK 语料覆盖；无真实数据才走 Mock 兜底，并新增 `is_mock` 标记便于下游识别过滤。
2. **`kol_data.json` #21**：`handle`/`channel_url` 修正为 `@bellafinance`；`fans` 修正为 `20W+`。
3. **`README.md`**：更新「抓取与中文转化」小节，使文档与新行为一致。
4. 已用旧 `output/data.json` 中的真实条目对修复后的函数做回归验证（真实标题不再被篡改，兜底分支正常打标）。

## 四、遗留风险（未在本次修复中处理，建议后续跟进）

- **全项目性问题**：`output/audit_report.md` 显示 237 条内容中 219 条（92.4%）为 `?v=mock` 伪链接 —— 没有 `channel_id` 或 RSS 失败的 KOL 仍走 Mock 兜底并生成伪链接。本次修复让它们带上了 `is_mock` 标记，但报告模板尚未基于该标记做过滤/显著标注。
- **#65 贝拉说美股**：channel_url 为 YouTube 搜索页占位、channel_id 缺失（审计 FAIL），疑似与 #21 重复或冒名，建议人工确认后删除或补全。
- Mock 兜底分支生成的内容仍会进入 DeepSeek 研判，建议后续对 `is_mock=True` 的条目跳过研判或在战报中标注"模拟演示数据"。

---

*本报告由排查过程整理，相关修复已同步提交。*
